# Google Forms integration (legacy)

Legacy `/app/contact/` and `/app/mhgmedia/` form pages have been retired. Those routes now redirect to `/contact/` and `/webops/`.

Live lead capture uses the **Apps Script Leads endpoint** — see [APPS_SCRIPT_LEADS_ENDPOINT.md](APPS_SCRIPT_LEADS_ENDPOINT.md) and [lib/forms/README.md](lib/forms/README.md).
