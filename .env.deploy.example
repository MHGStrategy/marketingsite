# Copy to .env.deploy.local (gitignored) for password-based deploy.
# SSH key auth: add ~/.ssh/id_ed25519.pub in SPanel → SSH Keys, then deploy without SSHPASS.
#
# DEPLOY_HOST=mhgstrategy
# DEPLOY_PATH=public_html/
# SSHPASS=your-scalahosting-ssh-password
